export type Sliders = { id: string; pic: string; link: string }
export type Albums = {
  id: string
  pic: string
  title: string
  username: string
}

export type Singer = {
  id: number
  mid: string
  name: string
  pic: string
}
